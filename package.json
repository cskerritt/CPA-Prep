{
  "name": "cpa-prep",
  "version": "1.0.0",
  "private": true,
  "description": "CPA Exam mastery system — knowledge base + dependency-free study app (planner, flashcards, practice MCQs, error log, readiness).",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node app/test/smoke.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
